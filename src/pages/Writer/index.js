import React from 'react';

import { style as PageStyle } from './style';

const Writer = () => (
    <div
        className={PageStyle}
    >
        <h1>Writer</h1>
    </div>
);

export default Writer;